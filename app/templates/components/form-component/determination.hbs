{{#field-group
    title=title identifier=title
    counter=model.determinations.length as |fieldGroup|
}}
    {{#fieldGroup.content}}
        {{#each model.determinations as |determination index|}}
            {{form-component/single-determination
                model=determination
                remove=(action "removeDetermination")

                index=(one-index index) 
            }}
        {{/each}}
     {{/fieldGroup.content}}
     {{#fieldGroup.toolbar}} 
        <button class="btn btn-primary btn-xs" {{action "addDetermination"}}>
            <i class="glyphicon glyphicon-plus"></i> {{t 'fields.labels.determination.add'}}
        </button>
     {{/fieldGroup.toolbar}}
{{/field-group}}
